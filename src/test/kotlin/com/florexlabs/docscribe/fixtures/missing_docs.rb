# frozen_string_literal: true

# A class with missing documentation.
class MissingDocs
  def greet(name)
    "Hello, #{name}!"
  end

  # @param a [Integer] the first number.
  def add(a, b)
    a + b
  end
end
